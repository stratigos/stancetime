import React from "react";
import { Link } from "react-router-dom";

const Header = () => {
  return(
    <header className="react-app-header text-center">
      <h1 className="react-app-title display-1">StanceTime</h1>
      <p class="text-muted">Breathe, Focus, Grow</p>
    </header>
  );
}

export default Header;
